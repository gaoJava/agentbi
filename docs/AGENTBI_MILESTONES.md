# AgentBI 大赛项目里程碑

## 2026-08-31 产品化交付计划

> 交付日期：2026-08-31（下周一）。本计划在 M0–M6 技术基线之上推进，不删除已经通过验证的安全编排、SuperSonic 适配、问答、报告与审计能力。

### 交付总览

| 编号 | 截止时间 | 里程碑 | 状态 | 交付结果 |
| --- | --- | --- | --- | --- |
| M7（产品化 M1） | 08-26 24:00 | 产品壳、登录与 RBAC | 已完成 | 独立登录页；管理员/普通用户菜单与数据范围隔离；三栏工作台骨架 |
| M8 | 08-27 24:00 | Superset 分析画布融合 | 待开始 | 中间区域承载仪表盘；编辑/拖拽入口；中文导航；加载与异常状态 |
| M9 | 08-28 24:00 | 分层下钻与 Agent 上下文 | 待开始 | 原图保留、纵向下钻链、面包屑、逐层问答、证据路径 |
| M10 | 08-29 24:00 | 安全、稳定性与自动化验收 | 待开始 | 越权与上下文伪造防护；降级/重试；回归测试；前端构建 |
| M11 | 08-30 18:00 | 大赛演示与交付包 | 待开始 | 5 分钟主演示、备用演示、评分映射、截图与一键启停 |
| Buffer | 08-31 12:00 | 最终联调与交付缓冲 | 待开始 | 只修复阻断问题，不再增加范围；生成最终交付版本 |

### M7 / 产品化 M1：产品壳、登录与 RBAC（已完成）

验收标准：

- [x] 登录页提供普通用户与管理员演示入口，并明确企业权限与数据隔离提示。
- [x] 身份与权限由服务端会话确定，浏览器不能通过修改角色字段提权。
- [x] 普通用户仅显示分析工作台、授权仪表盘和分析报告。
- [x] 管理员额外显示仪表盘管理、语义模型、数据源、用户与角色、审计与安全。
- [x] 普通用户只获取授权数据范围，管理员可查看全域治理信息。
- [x] 登录后进入“左侧功能菜单 + 中间分析画布 + 右侧 AgentBI”的三栏工作台。
- [x] 覆盖登录失败、未登录访问、退出登录和移动/窄屏基础状态。

验证证据：`25 passed`，Ruff 与 JavaScript 语法检查通过；真实浏览器验证普通用户
仅显示华东数据且无编辑/治理入口，管理员显示全部区域及治理入口；390px 视口无横向溢出。

### M8：Superset 分析画布融合（待开始）

验收标准：

- [ ] 中间区域可加载 Superset 仪表盘，产品导航与 Superset 内部导航不重复抢占空间。
- [ ] 提供查看态与编辑态；编辑态进入 Superset 原生图表构建/拖拽能力。
- [ ] 统一中文产品文案、空状态、加载状态和上游不可用提示。
- [ ] AgentBI 能稳定获取当前仪表盘、图表、筛选器、时间范围和选中数据点。
- [ ] Superset 不直接暴露在公网；嵌入令牌、CSRF 和允许域均由服务端控制。

### M9：分层下钻与 Agent 上下文（待开始）

验收标准：

- [ ] 柱状图、折线/面积图、饼/环图、散点图、地图/热力图、树图和表格支持数据点下钻。
- [ ] KPI、大数字、仪表盘、漏斗图和桑基图仅在配置默认下钻维度后开放入口。
- [ ] 文本、图片、Markdown、分割线和筛选器不显示下钻入口。
- [ ] 下钻时保留原始图，按“第 0 层 → 第 1 层 → 第 N 层”纵向展示。
- [ ] 每层记录图表、维度、值、筛选条件和语义模型，并支持返回上一层或从任意层重新分支。
- [ ] 每层均可调用 AgentBI；问答请求携带服务端校验后的完整下钻路径。
- [ ] 报告包含结论、建议、证据路径和来源仪表盘回链。

### M10：安全、稳定性与自动化验收（待开始）

验收标准：

- [ ] 覆盖登录、RBAC、行级数据范围、下钻上下文伪造、注入和敏感信息泄露测试。
- [ ] Superset/SuperSonic 超时或不可用时提供明确的可恢复状态，不造成整页白屏。
- [ ] 保留幂等、限流、超时、结果截断、SQL 指纹和结构化审计能力。
- [ ] Python 测试、Ruff、TypeScript 类型检查和 Webpack 生产构建全部通过。
- [ ] 一键启动后完成登录 → 仪表盘 → 两级下钻 → Agent 问答 → 报告的端到端验证。

### M11：大赛演示与交付包（待开始）

验收标准：

- [ ] 主流程控制在 5 分钟内，突出交互、架构、稳定性、安全和创新。
- [ ] 准备上游故障时仍可展示的备用数据与录屏/截图证据。
- [ ] README、架构图、运行手册、演示脚本、演示账号和访问地址保持一致。
- [ ] 关键工程代码包含解释设计意图、安全边界和异常处理的有效注释。
- [ ] 形成最终验收记录，所有未完成项如实列明，不提前标记完成。

### 范围冻结与实施原则

- 直接调整 `agentbi` 项目；复用当前编排器与 Superset 扩展，不重写已验证的安全核心。
- 新界面优先实现比赛主演示闭环；非主演示功能进入交付后待办，不挤占稳定性时间。
- 每完成一个里程碑，必须同时更新本文件状态、自动化验证结果和可复现证据。
- 08-30 18:00 后冻结新功能；08-31 仅处理交付阻断问题。

> 项目目标：基于 Apache Superset 与 SuperSonic 构建可感知大屏上下文、可追问下钻、可验证证据、可生成报告的安全 AgentBI。
>
> 状态规则：`待开始` → `进行中` → `已完成`。只有验收标准全部通过并留下验证证据，才标记为 `已完成`。

## 总体进度

| 编号 | 里程碑 | 状态 | 主要评分项 |
| --- | --- | --- | --- |
| M0 | 项目基线与总体架构 | 已完成 | 架构、代码规范 |
| M1 | 安全编排器与审计证据链 | 已完成 | 架构、安全、稳定性 |
| M2 | SuperSonic 语义查询适配 | 已完成 | 功能、创新、稳定性 |
| M3 | Superset 大屏内嵌 AI 洞察 | 已完成 | 产品交互、架构 |
| M4 | 多轮追问、下钻与报告生成 | 已完成 | 创新、使用功能 |
| M5 | 稳定性、安全与自动化验收 | 已完成 | 流程稳定性、安全 |
| M6 | 大赛演示、文档与交付包 | 已完成 | 产品交互、代码注释、综合展示 |

## M0：项目基线与总体架构（已完成）

验收标准：

- [x] 完成 Superset、SuperSonic 两个开源项目的职责拆分。
- [x] 采用“Superset 展示层 + AgentBI 编排层 + SuperSonic 语义层”的三层架构。
- [x] 工作区代码、部署配置、测试与扩展目录完成初始化。
- [x] 明确不直接拼接或执行模型生成 SQL，所有查询必须经过受治理语义接口。

验证证据：AgentBI FastAPI 工程、Superset 扩展工程和部署目录已经建立。

## M1：安全编排器与审计证据链（已完成）

验收标准：

- [x] 请求使用 API Key 验证，并限制问题、筛选器、角色和上下文长度。
- [x] 服务端注入用户角色，拒绝前端伪造授权信息。
- [x] 对上游响应设置大小、超时和错误边界。
- [x] 返回请求编号、查询编号、耗时、行数和 SQL 指纹，不向界面泄露原始 SQL。
- [x] 单元测试覆盖身份、输入验证和编排主流程。

验证证据：本地测试曾达到 9 项通过；后续每轮合并继续执行完整回归。

## M2：SuperSonic 语义查询适配（已完成）

验收标准：

- [x] 完成 SuperSonic 登录令牌、自然语言解析与查询执行适配。
- [x] 仅选择包含受治理 `querySQL` 的语义候选，拒绝外部网页插件候选。
- [x] 使用真实问题“alice 停留时长”跑通语义模型并返回数据。
- [x] 查询结果携带可核验的证据元数据。

验证证据：真实模型查询返回 6 行结果，并生成查询编号、耗时与 SQL 指纹。

## M3：Superset 大屏内嵌 AI 洞察（已完成）

验收标准：

- [x] Superset 后端发现并加载 `agentbi.insight-pilot` 扩展。
- [x] 扩展代理复用 Superset 当前用户和角色，并加入 CSRF 防护。
- [x] 前端实现大屏 ID、图表/数据集、时间范围与筛选条件的上下文桥接。
- [x] 实现悬浮入口、快捷问题、分析步骤、结果表格与审计信息面板。
- [x] 修复浏览器模块联邦加载问题，确保“AI 洞察”入口真实可见。
- [x] 在真实 Superset 大屏内完成一次端到端问答并保存截图证据。
- [x] 验证窄屏布局不遮挡、不溢出且能正常操作。

验证证据：真实 Sales Dashboard 中已显示“AI 洞察·已连接”；问题“alice 停留时长”经 Superset 会话、CSRF 代理、AgentBI 编排器和 SuperSonic 返回 6 行结果并展示审计编号。390px 等效窄屏下，面板和内容区均无横向溢出，问答与报告正常返回。

## M4：多轮追问、下钻与报告生成（已完成）

验收标准：

- [x] 保存会话上下文，支持对上一轮结论继续追问。
- [x] 支持从图表选择或分析结果发起维度下钻。
- [x] 实现“现象—原因—建议—证据”的结构化洞察卡片。
- [x] 支持生成 Markdown 报告，并记录来源、筛选条件和生成时间。
- [x] 报告中的关键结论可回跳到对应 Superset 大屏。

验证证据：AgentBI 生成与 Superset 用户绑定的随机会话，追问“哪一天停留时长最高？”复用同一会话并返回 1 行结果；跨用户会话复用会被拒绝。下钻问题、结构化观察/建议、Markdown 报告及来源大屏回链均验证通过。

## M5：稳定性、安全与自动化验收（已完成）

验收标准：

- [x] 单元、集成和端到端测试全部通过。
- [x] 对超时、无结果、歧义问题、上游不可用和重复提交提供可恢复体验。
- [x] 增加请求幂等、限流、重试边界、健康检查和结构化日志。
- [x] 完成注入、越权、敏感信息泄露、危险链接和大响应攻击检查。
- [x] 清除演示硬编码密钥，提供生产环境密钥轮换与最小权限配置。
- [x] 形成一条可重复执行的自动化验收命令。

验证证据：最终 `scripts/verify.ps1` 执行 18 项测试全部通过，Ruff、TypeScript 和 Webpack 生产构建通过；真实浏览器双轮 E2E、相同请求幂等复用、403 安全错误映射和结构化追踪日志均通过。

## M6：大赛演示、文档与交付包（已完成）

验收标准：

- [x] 提供 Windows 一键启动/停止脚本和清晰访问地址。
- [x] README 覆盖架构、部署、配置、故障排查、安全说明和演示流程。
- [x] 核心模块补齐有价值的中文/英文代码注释，避免无意义逐行注释。
- [x] 准备 5 分钟主演示和异常恢复备用演示。
- [x] 按评分表完成自评：交互 20、架构 30、注释 10、稳定性 25、创新/安全加分。
- [x] 标明 Codex 参与的需求拆解、编码、测试和修复证据。

验证证据：一键启动脚本在已有服务场景通过，所有 PowerShell 脚本语法检查通过；README、开发运行手册和 `competition-demo.md` 已覆盖启动、地址、安全上线、5 分钟演示、备用演示、评分自评与 Codex 工程证据。

## 今晚执行顺序

1. 完成 M3：扩展加载修复、真实问答、桌面与窄屏截图。
2. 开始 M4：优先实现多轮上下文和结构化报告最小闭环。
3. 推进 M5：完整回归、静态检查、关键安全用例与故障恢复。
4. 更新启动说明和当前里程碑状态；未达到验收标准的项目不提前标记完成。

## 状态更新记录

| 时间 | 更新 |
| --- | --- |
| 2026-08-25 | 建立里程碑文档；M0、M1、M2 已完成，M3 进行中。 |
| 2026-08-25 | 修复扩展仅在初始化路由挂载的问题；Superset 大屏端到端真实问答通过，M3 剩余窄屏验收。 |
| 2026-08-25 | 完成真实多轮追问、结果下钻、结构化洞察和可审计报告回链，M4 验收完成。 |
| 2026-08-25 | 完成 390px 等效窄屏问答验收，M3 验收完成。 |
| 2026-08-25 | 18 项测试、静态检查、前端生产构建、幂等/限流/安全回归通过，M5 验收完成。 |
| 2026-08-25 | 完成一键启停、README、5 分钟演示、备用演示与评分自评，M6 验收完成。 |
| 2026-08-26 | 产品化 M1 完成：独立登录、签名会话、RBAC 菜单、数据范围隔离和三栏工作台通过自动化与浏览器验收。 |
| 2026-08-26 | 按确认原型完成视觉校准：登录背景、统一菜单图标、普通用户/管理员“我的权限”、治理概览及三级下钻页面均通过 1280×720 浏览器对照验收。 |
| 2026-08-26 | 身份系统数据库化：完成 SQLite/PostgreSQL 兼容 RBAC、密码哈希、可撤销会话、登录审计及显式“切换账号/退出登录”交互。 |
| 2026-08-26 | 完成配置化图表下钻入口：工作台图表三点菜单支持设置/进入下钻，销售趋势与销售结构可携带不同原始图、维度路径和 Agent 证据上下文。 |
| 2026-08-26 | 增加管理员“下钻配置”页面：可视化展示注册图表、指标、语义模型、原始图类型、维度路径、校验状态，并支持直达下钻分析。 |
| 2026-08-27 | 完成新增图表闭环：管理员向导将图表与下钻路径原子写入数据库，普通用户工作台动态加载图表、三点菜单、分层下钻与 Agent 证据路径。 |
| 2026-08-27 | 修复新增图表下钻路径解析：支持英文/中文逗号及 `→`、`>` 箭头分隔，并完成页面创建与重复图表 ID 提示验收。 |
| 2026-08-27 | 完成图表与下钻配置生命周期：支持修改、下线/上线、先下线后删除、内置配置保护、服务端 RBAC/CSRF 校验与审计记录。 |
