# Copy to .env only when overriding the local database defaults.
# Do not submit a populated password to a public repository.
POSTGRES_PASSWORD=replace-with-a-strong-local-password
POSTGRES_PORT=5432

